<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Climate Connections</title>
    <link href=/style.css rel=stylesheet type=text/css>
  </head>
  <body>
    <script type="text/javascript">
      let exp = <%= user.points %> / 6000;
      var myElement = document.querySelector(".pie");
      myElement.style.backgroundimage = "linear-gradient(to top, transparent 29%, #1e8feb 0);";
    </script>
    <div class="menu-container">
      <div class="menu">
        <div class="links">
        <a class='navigation'href="/dashboard">Dashboard</a>
        <a class='navigation'href="/profile">Profile</a>
      </div>
      <a class="login navigation"href="#">Sign In</a>
    </div>
    </div>

    <h1 class=level>Level <%= user.level %></h1>
    <div class="space"></div>
    <div class=flex>
      <img class='center' src="<%= user.profileImgUrl %>" alt="Your account information">
      <div class="pie"></div>
    </div>
    <div class=points> <div class="bigNumber"><%= user.points %></div>Points</div>
    <h1 class="level"><%= user.name %></h1>
    <div class="stats">
        You have <div class=numbers><%= user.points %></div> points
    </div>
    <div class="stats">
        You used <div class=numbers><%= user.watts %></div> Watts today
    </div>
    <div class="stats">
        You moved up <div class=numbers><%= user.oldRank - user.rank %></div>spots to rank<div class=numbers><%= user.rank %></div>
    </div>
  </body>
</html>
